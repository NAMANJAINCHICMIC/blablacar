export interface signUpData  {
    email: string | null,
     first_name: string | null,
     last_name: string | null,
      dob: string | null,
// email ?: string,
// dob ?: string,
// first_name ?: string,
// last_name ?: string,
password ?: string,
}